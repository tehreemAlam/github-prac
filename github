expenses = []

while True:
    item = input("\nEnter expense name (or 'done' to finish): ")

    if item.lower() == "done":
        break

    amount = float(input("Enter amount: "))

    expenses.append({
        "item": item,
        "amount": amount
    })

total = 0

print("\n--- EXPENSES ---")

for expense in expenses:
    print(expense["item"], ":", expense["amount"])
    total += expense["amount"]

print("----------------")
print("Total:", total)


total = 0

expenses = []

expenses.append(["Food", 500])
expenses.append(["Transport", 200])
expenses.append(["Books", 800])

for expense in expenses:
    print(expense[0], ":", expense[1])

for expense in expenses:
    total = total + expense[1]

print("Total expenses:", total)
